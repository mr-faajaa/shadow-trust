# 2026-02-07 - ShadowTrust Hackathon & Git Lessons

## Major Git Pollution Incident

**What happened:**
- Ran `git add -A` from `/home/ubuntu/.openclaw/workspace/` instead of project directory
- Committed 635 OpenClaw workspace files (skills, MEMORY.md, AGENTS.md, SOUL.md) to ShadowTrust repo
- Polluted clean project with OpenClaw files

**Resolution:**
- Created clean clone in `/tmp/shadow-trust-clean`
- Copied only ShadowTrust files (dashboard/, README.md, etc.)
- Force-pushed to GitHub
- Deleted master branch (only main remains)

**Documentation added:**
- AGENTS.md: Git repository management rules
- MEMORY.md: Git workspace pollution incident

**User frustration:** Multiple times I acted without verifying first (checking if commit worked, checking if branch deleted). User explicitly said "verify with logic guard before just tell me what's not true."

## ShadowTrust Project Status

**Clean repo structure:**
```
shadow-trust/
├── README.md
├── IMPLEMENTATION_SUMMARY.md
├── VERCEL_DEPLOY.md
└── dashboard/ (Next.js only)
    ├── src/
    │   ├── app/ (page.tsx, api routes)
    │   ├── components/reactbits/
    │   └── lib/
    ├── public/favicon.svg
    └── vercel.json
```

**Features built:**
- ReactBits components: FaultyTerminal, DecryptedText, CountUp, GlitchText
- Solana RPC via native fetch (no npm dependency)
- x402 payment integration
- Real wallet: Not Agent (CFaXxN9fqowBQUa5bjYeHejHu8kUZGoqLJ1zMC1QEsKa)

**Vercel setup:**
- Root directory: `dashboard`
- Framework: Next.js
- Removed root-level Express API files that confused detection

## Git Rules (from AGENTS.md)

1. Always `cd` into project directory first
2. Use explicit paths, never wildcard `git add -A` from workspace root
3. Check `git status` before committing
4. Project repos are NOT part of OpenClaw

## User Preferences Confirmed

- User: Stage (Mr. Faajaa 🥷)
- Timezone: UK (GMT/BST)
- X/Twitter account: Available for my use
- Skills preference: vercel-labs, softaworks, obra, anthropics sources

## OpenClaw Updated

- Updated to v2026.2.6-3 via npm
- ClawdStrike security skill installed
- agent-browser installed
